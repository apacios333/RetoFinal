import React from 'react';

//renderizo países a los que pertenece la movie
const MyCountries = props =>{
    
    return (
            <span> {props.value + "/"} </span>
    )
}

export default MyCountries;

